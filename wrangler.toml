name = "llm-visualisation"
compatibility_date = "2024-01-01"

# Workers static assets — required for `npx wrangler deploy` in Cloudflare CI
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
